# pi-subagents

Shared in-process subagent runtime foundation for Pi extensions.

This package currently provides the definition registry, public builtin configuration loader, and current-session runtime record lifecycle used by later subagent features.
